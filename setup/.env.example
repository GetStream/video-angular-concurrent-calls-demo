# Stream credentials for the demo app.
# Copy this file to `.env` and fill in the secret, then run `npm run setup`.
#
# Find both in the Stream dashboard: https://dashboard.getstream.io
# The secret is server-side only — it must never reach the browser.
STREAM_API_KEY=cr6eejhcddch
STREAM_API_SECRET=
