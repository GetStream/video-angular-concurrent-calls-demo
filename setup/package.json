{
  "name": "video-angular-demo-setup",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "One-shot server-side setup for the Stream Video + Angular proctored-exam demo",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "setup": "tsx src/setup.ts",
    "tokens": "tsx src/tokens.ts",
    "typecheck": "tsc --noEmit",
    "verify": "tsx src/verify.ts"
  },
  "dependencies": {
    "@stream-io/node-sdk": "^0.8.4",
    "dotenv": "^17.0.0"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "~5.9.0"
  }
}
