# stream-chat-angular's transitive ngx-float-ui has no Angular 22 build yet, so npm's peer
# resolution fails without this. Stream documents exactly this flag for Angular 22.
# Kept in .npmrc rather than as a remembered CLI flag so every install behaves the same.
legacy-peer-deps=true
