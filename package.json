{
  "name": "video-angular-demo-extended",
  "version": "1.0.0",
  "private": true,
  "description": "Customer-facing demo: integrating Stream Video with Angular (proctored exam calls)",
  "scripts": {
    "setup": "npm --prefix setup install && npm --prefix setup run setup",
    "start": "npm --prefix app start"
  }
}
